export const clientsAPI = {
  getAll: () => api.get<Client[]>('/clients'),
  create: (nom: string) => api.post<Client>('/clients', { nom }),
};

export const appor teursAPI = {
  getAll: () => api.get<Apporteur[]>('/apporteurs'),
  create: (data: Partial<Apporteur>) => api.post<Apporteur>('/apporteurs', data),
};

export default api;
